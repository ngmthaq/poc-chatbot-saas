export const QUEUE_NAMES = {
  DEFAULT: 'default',
  EMAIL: 'email',
  NOTIFICATIONS: 'notifications',
} as const;

export const DEFAULT_JOB_ATTEMPTS = 3;

export const DEFAULT_JOB_BACKOFF_MS = 5000;
