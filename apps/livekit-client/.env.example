# ========================================================================
# LiveKit Client Environment Variables
# These variables configure the LiveKit client's connection to the LiveKit server and other settings.
# Get your environment values from your LiveKit server configuration (livekit-infra/.env) and fill in
# any additional settings as needed.
# ========================================================================
VITE_API_BASE_URL=http://localhost:3000
VITE_LIVEKIT_URL=ws://localhost:7880
