import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const ControlBarRoot = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  gap: 16,
});
