# agent-worker environment template.
#
# This file is a TEMPLATE ONLY — every value below is intentionally empty.
# Create the real `.env` from it (`cp .env.example .env`) and fill in real
# values by hand. DO NOT commit `.env`: it holds live LiveKit credentials
# and is gitignored.

# LiveKit server WebSocket URL the agent worker connects to.
# Local dev on Docker Desktop (recommended default): ws://host.docker.internal:7880
#   `localhost` inside the worker container resolves to the container itself,
#   not the host, so it CANNOT reach a sibling livekit-server stack. Use
#   `host.docker.internal` (macOS/Windows Docker Desktop) — or attach the
#   worker to the `livekit-server` compose network on Linux hosts.
# Production: wss://<your-project>.livekit.cloud
LIVEKIT_URL=

# LiveKit project API key. Required.
LIVEKIT_API_KEY=

# LiveKit project API secret. Required. Never commit a real value.
LIVEKIT_API_SECRET=

# Dispatch name the worker registers with. Backend-service must pass this
# value to AgentDispatchClient.createDispatch() to invoke this worker.
# Setting AGENT_NAME enables explicit dispatch (automatic dispatch disabled).
AGENT_NAME=

# Pino log level: trace | debug | info | warn | error | fatal. Defaults to "info" when unset.
LOG_LEVEL=
